language: python
python:
    # We don't actually use the Travis Python, but this keeps it organized.
    - "2.7"
    - "3.5"
services:
    - postgresql
sudo: required
install:
    # We do this conditionally because it saves us some downloading if the
    # version is the same.
    - if [[ "$TRAVIS_PYTHON_VERSION" == "2.7" ]]; then
          wget https://repo.continuum.io/miniconda/Miniconda2-latest-Linux-x86_64.sh -O miniconda.sh;
      else
          wget https://repo.continuum.io/miniconda/Miniconda3-latest-Linux-x86_64.sh -O miniconda.sh;
      fi
    # install gfortran-5
    - sudo apt-get install gfortran-5
    - "ls -l `which gfortran`"
    - "GFORTRAN_BIN=`which gfortran`"
    # make sure we use an 'innocent' default matplotlib environment
    - touch matplotlibrc
    - bash miniconda.sh -b -p $HOME/miniconda
    - export PATH="$HOME/miniconda/bin:$PATH"
    - pip install pyyaml  # is somehow not included in the current conda installation
    - hash -r
    - conda config --set always_yes yes --set changeps1 no
    - conda update -q conda
    # Useful for debugging any issues with conda
    - conda info -a
    - conda create -n test python="$TRAVIS_PYTHON_VERSION" scipy numpy statsmodels matplotlib xarray
    - source activate test
    - pip install model-organization
    - pip install coveralls pytest
    # use latest development version
    # NOTE: Remove this when psyplot is stable enough
    - git clone "https://github.com/Chilipp/psyplot.git" -b dev
    - cd psyplot && python setup.py install && cd .. && rm -rf psyplot
    - python setup.py install
    # export the environment for debugging
    - conda env export -n test
    - "ls -l `which gfortran`"

before_script:
    - "export DISPLAY=:99.0"
    - "sh -e /etc/init.d/xvfb start"
    - sleep 3 # give xvfb some time to start

script:
    - py.test -xv tests
    #- coverage run --source=gwgen setup.py test
#
#after_success:
#    coveralls
